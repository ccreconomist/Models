SELECT
    TARJETA,
    SUBSTRING(CLIENTE, 10, LEN(CLIENTE)) AS CLIENTE_LIMPIO,
    CUENTA,
    NOMBRE
    ---LEFT(REPLACE(SUBSTRING(CUENTA, 4, LEN(CUENTA)), '0', ''), 6) AS NO_DE_CUENTA
FROM
    IT_CARTERA_GCC
WHERE
    CUENTA IN ('0004698490004478000', '0004698490004730000');