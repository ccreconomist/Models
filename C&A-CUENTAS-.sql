SELECT 
    ACTIVACION,
    ULTC,
    CUENTA,
    STUFF(CUENTA, 1, 3, '') AS MOD,
    LEFT(STUFF(CUENTA, 1, 3, ''), 6) AS CUENTA2
FROM
    IT_CARTERA_PLCC_EXT
WHERE
    ACTIVACION >= '2022-09-01 00:00:00.000'
    AND ACTIVACION <= '2023-09-25 00:00:00.000';

